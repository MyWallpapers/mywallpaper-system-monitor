name: Publish MyWallpaper release through admission-v1

on:
  push:
    tags: ['v*']

permissions:
  contents: read

jobs:
  release:
    if: github.event.repository.private == false
    concurrency:
      group: mywallpaper-release-${{ github.repository_id }}-${{ github.ref }}
      cancel-in-progress: false
    permissions:
      actions: read
      artifact-metadata: write
      attestations: write
      contents: write
      id-token: write
    uses: MyWallpapers/native-addon-toolchain/.github/workflows/native-addon-build.yml@fce289da71538f7292bd9324b4f02e8ea469cea8
    with:
      channel: development
